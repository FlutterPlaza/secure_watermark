name: secure_watermark
description: "Visible & invisible watermarking for Flutter. Overlay watermarks deter screenshots; forensic LSB watermarks embed traceable data in pixels."
version: 0.3.1
homepage: https://flutterplaza.com
repository: https://github.com/FlutterPlaza/secure_watermark
issue_tracker: https://github.com/FlutterPlaza/secure_watermark/issues
topics:
  - security
  - watermark
  - dlp
  - forensic

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=3.19.0"

dependencies:
  args: ^2.5.0
  flutter:
    sdk: flutter
  image: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=4.0.0 <6.0.0"

name: secure_watermark
description: Visible watermark overlay for Flutter. Renders user ID, email, or timestamp over protected content to deter screenshots and trace leaks.
version: 0.1.0
homepage: https://flutterplaza.com
repository: https://github.com/FlutterPlaza/secure_watermark
issue_tracker: https://github.com/FlutterPlaza/secure_watermark/issues
topics:
  - security
  - watermark
  - dlp

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=4.0.0 <6.0.0"
